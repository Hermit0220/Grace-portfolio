const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({headless: true}); const page = await browser.newPage(); await page.goto('http://localhost:3000'); await page.click('#login-icon'); await page.fill('#login-username', 'Grace2006'); await page.fill('#login-password', '2006'); await page.click('#login-form button[type="submit"]'); await page.waitForTimeout(1000); const isVisible = await page.evaluate(() => { const btn = document.getElementById('p5-manage-tracks-btn'); return btn && btn.style.display !== 'none'; }); console.log('Manage Tracks Button Visible:', isVisible); await browser.close(); })();
